Si lo intentamos correr local, el mae da un error de Chrome de Cross Origin.

Esto se puede solucionar instalando NPM y después hacer

npm install http-server -g

Al momento de ver el treemap corriendo, se debe correr con el siguiente comando

http-server . -o

Lo que hace es que levanta el localhost y ya se puede manejar 